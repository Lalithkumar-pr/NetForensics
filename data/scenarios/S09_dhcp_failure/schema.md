# Synthetic Network Evidence Schema v1.0

`topology.json`: devices, services, links and status.
`events.log`: UTC timestamped device/service events.
`traffic.pcap`: valid classic libpcap with DHCP and ICMP evidence.
`ground_truth.json`: injected root cause and expected propagation.
